diff --git a/CMakeLists.txt b/CMakeLists.txt
index 3c7c1b8a3..1c474ad9c 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -20,13 +20,397 @@ option(BUILD_EXAMPLE_MODULES "Build example modules" OFF)
 
 set(CMAKE_MODULE_PATH ${CMAKE_MODULE_PATH} "${CMAKE_SOURCE_DIR}/cmake/Modules/")
 project("valkey")
-add_compile_options(-Wundef)
+
+include(FetchContent)
+if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/../auto-win-msvc")
+    FetchContent_Declare(
+        auto_win_msvc
+        SOURCE_DIR "${CMAKE_CURRENT_SOURCE_DIR}/../auto-win-msvc"
+    )
+else()
+    FetchContent_Declare(
+        auto_win_msvc
+        GIT_REPOSITORY https://github.com/SamuelMarks/auto-win-msvc.git
+        GIT_TAG        master
+    )
+endif()
+FetchContent_MakeAvailable(auto_win_msvc)
+set(INSTALL_GTEST OFF CACHE BOOL "Disable GTest installation" FORCE)
+if(MSVC)
+    link_libraries(auto-win-msvc)
+endif()
+if(NOT MSVC)
+    add_compile_options(-Wundef)
+else()
+    file(WRITE "${CMAKE_BINARY_DIR}/msvc_compat.h" "
+#ifndef MSVC_COMPAT_H
+#define MSVC_COMPAT_H
+#ifndef WIN32_LEAN_AND_MEAN
+#define WIN32_LEAN_AND_MEAN
+#endif
+#define NOMINMAX
+#define NOUSER
+#define NOGDI
+#define __attribute__(...)
+#define __attribute(...)
+#undef LoadString
+#undef ERROR
+#undef WORD
+#define _OFF_T_DEFINED
+#define _off_t __int64
+#define off_t __int64
+#define pthread_self() win_pthread_self()
+#define F_GETFL 3
+#define F_SETFL 4
+#define F_GETFD 1
+#define F_SETFD 2
+#define O_NONBLOCK 0x4000
+#define FD_CLOEXEC 1
+#define O_CLOEXEC 0x80000
+#define AF_LOCAL 1
+#define SIGPIPE 13
+#define IOV_MAX 1024
+#define PATH_MAX 4096
+#include <stdint.h>
+#include <posix-time.h>
+#include <malloc.h>
+#include <posix-stat.h>
+#include <posix-types.h>
+#include <strings.h>
+#include <posix-core.h>
+#include <posix-signal.h>
+#include <posix-sockets.h>
+#include <posix-syslog.h>
+#include <posix-dlfcn.h>
+#include <posix-arpa-inet.h>
+#include <posix-dirent.h>
+#include <posix-signal-ext.h>
+#ifdef __cplusplus
+#include <atomic>
+#include <cstdio>
+#include <cstdlib>
+#include <cstddef>
+#include <cstring>
+#include <ctime>
+#include <climits>
+#include <cmath>
+#endif
+#ifndef _SC_PAGESIZE
+#define _SC_PAGESIZE 1
+#endif
+#ifndef ESOCKTNOSUPPORT
+#define ESOCKTNOSUPPORT 121
+#endif
+#ifndef EPFNOSUPPORT
+#define EPFNOSUPPORT 122
+#endif
+#ifndef SIGHUP
+#define SIGHUP 1
+#endif
+#ifndef SIGSTOP
+#define SIGSTOP 19
+#endif
+#ifndef SIGUSR1
+#define SIGUSR1 10
+#endif
+#ifndef SIGTRAP
+#define SIGTRAP 5
+#endif
+#ifndef SIG_BLOCK
+#define SIG_BLOCK 0
+#endif
+#ifndef SIGKILL
+#define SIGKILL 9
+#endif
+#ifndef SIGBUS
+#define SIGBUS 10
+#endif
+#ifndef SI_USER
+#define SI_USER 0
+#endif
+#ifndef M_PI
+#define M_PI 3.14159265358979323846
+#endif
+#ifndef SHUT_RDWR
+#define SHUT_RDWR 2
+#endif
+
+#ifndef SSIZE_MAX
+#ifdef _WIN64
+#define SSIZE_MAX 9223372036854775807LL
+#else
+#define SSIZE_MAX 2147483647
+#endif
+#endif
+#ifndef BYTE_ORDER
+#define LITTLE_ENDIAN 1234
+#define BIG_ENDIAN 4321
+#define BYTE_ORDER LITTLE_ENDIAN
+#endif
+#undef LoadString
+#undef ERROR
+#undef WORD
+#endif
+")
+    
+    # Generate missing POSIX headers as stubs mapping to auto-win-msvc
+    set(POSIX_STUBS_DIR "${CMAKE_BINARY_DIR}/posix_stubs")
+    file(MAKE_DIRECTORY "${POSIX_STUBS_DIR}" "${POSIX_STUBS_DIR}/sys" "${POSIX_STUBS_DIR}/netinet")
+    file(WRITE "${POSIX_STUBS_DIR}/netinet/in.h" "#include <posix-netinet-in.h>\n")
+    file(WRITE "${POSIX_STUBS_DIR}/netinet/tcp.h" "#include <posix-netinet-tcp.h>\n")
+    file(WRITE "${POSIX_STUBS_DIR}/sys/param.h" "#include <bsd-sys-param.h>\n")
+    file(WRITE "${POSIX_STUBS_DIR}/termios.h" "#include <posix-termios.h>\n")
+    file(WRITE "${POSIX_STUBS_DIR}/strings.h" "#include <posix-strings.h>\n")
+    file(WRITE "${POSIX_STUBS_DIR}/sys/time.h" "#include <posix-time.h>
+#include <malloc.h>\n")
+    # time.h stub removed
+    file(WRITE "${POSIX_STUBS_DIR}/endian.h" "#include <bsd-sys-endian.h>\n")
+    file(WRITE "${POSIX_STUBS_DIR}/sys/socket.h" "#include <posix-sockets.h>\n")
+    file(WRITE "${POSIX_STUBS_DIR}/poll.h" "#include <posix-poll.h>\n")
+    file(WRITE "${POSIX_STUBS_DIR}/sys/un.h" "#include <posix-sys-un.h>\n")
+    file(MAKE_DIRECTORY "${POSIX_STUBS_DIR}/arpa")
+    file(WRITE "${POSIX_STUBS_DIR}/arpa/inet.h" "#include <posix-arpa-inet.h>\n")
+    file(WRITE "${POSIX_STUBS_DIR}/netdb.h" "#include <posix-netdb.h>\n")
+    file(WRITE "${POSIX_STUBS_DIR}/sys/resource.h" "#include <posix-sys-resource.h>\n")
+    file(WRITE "${POSIX_STUBS_DIR}/pthread.h" "#include <posix-pthread.h>\n")
+    file(WRITE "${POSIX_STUBS_DIR}/dlfcn.h" "#include <posix-dlfcn.h>\n")
+    file(WRITE "${POSIX_STUBS_DIR}/sys/socket.h" "#include <posix-sockets.h>\n")
+    file(WRITE "${POSIX_STUBS_DIR}/poll.h" "#include <posix-poll.h>\n")
+    file(WRITE "${POSIX_STUBS_DIR}/sys/un.h" "#include <posix-sys-un.h>\n")
+    file(MAKE_DIRECTORY "${POSIX_STUBS_DIR}/arpa")
+    file(WRITE "${POSIX_STUBS_DIR}/arpa/inet.h" "#include <posix-arpa-inet.h>\n")
+    file(WRITE "${POSIX_STUBS_DIR}/sys/resource.h" "#include <posix-sys-resource.h>\n")
+    file(WRITE "${POSIX_STUBS_DIR}/pthread.h" "#include <posix-pthread.h>\n")
+    file(WRITE "${POSIX_STUBS_DIR}/dlfcn.h" "#include <posix-dlfcn.h>\n")
+    file(WRITE "${POSIX_STUBS_DIR}/pwd.h" "#include <posix-pwdgrp.h>\n")
+    file(WRITE "${POSIX_STUBS_DIR}/grp.h" "#include <posix-pwdgrp.h>\n")
+    file(WRITE "${POSIX_STUBS_DIR}/dirent.h" "#include <posix-dirent.h>\n")
+    file(WRITE "${POSIX_STUBS_DIR}/libgen.h" "#include <posix-libgen.h>\n")
+    file(WRITE "${POSIX_STUBS_DIR}/sys/file.h" "#include <bsd-sys-file.h>\n")
+    file(WRITE "${POSIX_STUBS_DIR}/regex.h" "#include <posix-regex.h>\n")
+    file(WRITE "${POSIX_STUBS_DIR}/sys/mman.h" "#include <posix-mman.h>\n")
+    file(WRITE "${POSIX_STUBS_DIR}/glob.h" "#include <posix-glob.h>\n")
+    file(WRITE "${POSIX_STUBS_DIR}/sys/ioctl.h" "#include <posix-sys-ioctl.h>\n")
+    file(WRITE "${POSIX_STUBS_DIR}/sys/uio.h" "#include <posix-sys-uio.h>\n")
+    file(WRITE "${POSIX_STUBS_DIR}/sys/wait.h" "#include <posix-wait.h>\n")
+    file(WRITE "${POSIX_STUBS_DIR}/sys/utsname.h" "#include <posix-utsname.h>\n")
+    file(WRITE "${POSIX_STUBS_DIR}/syslog.h" "#include <posix-syslog.h>\n")
+    file(WRITE "${POSIX_STUBS_DIR}/unistd.h" "
+#include <io.h>
+#include <process.h>
+#ifndef F_OK
+#define F_OK 0
+#define W_OK 2
+#define R_OK 4
+#define X_OK 4
+#endif
+#define STDIN_FILENO 0
+#define STDOUT_FILENO 1
+#define STDERR_FILENO 2
+")
+    file(WRITE "${POSIX_STUBS_DIR}/sched.h" "#include <posix-sched.h>\n")
+    include_directories(BEFORE "${POSIX_STUBS_DIR}")
+    
+    file(APPEND "${CMAKE_BINARY_DIR}/msvc_compat.h" "
+#undef _S_IEXEC
+#define _S_IEXEC 00100
+#undef _S_IREAD
+#define _S_IREAD 00400
+#undef _S_IWRITE
+#define _S_IWRITE 00200
+
+#include <stdlib.h>
+#include <intrin.h>
+#include <fcntl.h>
+static inline int __builtin_clz(unsigned int x) { unsigned long r; if (_BitScanReverse(&r, x)) return 31 - r; return 32; }
+static inline int __builtin_clzll(unsigned long long x) { unsigned long r; if (_BitScanReverse64(&r, x)) return 63 - r; return 64; }
+#define __builtin_clzl __builtin_clzll
+static inline int __builtin_ctz(unsigned int x) { unsigned long r; if (_BitScanForward(&r, x)) return r; return 32; }
+static inline int __builtin_ctzll(unsigned long long x) { unsigned long r; if (_BitScanForward64(&r, x)) return r; return 64; }
+#define __builtin_ctzl __builtin_ctzll
+static inline int __builtin_popcount(unsigned int x) { return __popcnt(x); }
+static inline uint64_t __builtin_bswap64(uint64_t x) { return _byteswap_uint64(x); }
+#define random rand
+#define srandom srand
+static inline char* strndup(const char *s, size_t n) {
+    size_t len = 0;
+    while (len < n && s[len] != '\\0') len++;
+    char *new_s = (char*)malloc(len + 1);
+    if (new_s == NULL) return NULL;
+    memcpy(new_s, s, len);
+    new_s[len] = '\\0';
+    return new_s;
+}
+static inline int mkstemp(char *tmpl) {
+    if (_mktemp_s(tmpl, strlen(tmpl) + 1) != 0) return -1;
+    return _open(tmpl, _O_RDWR | _O_CREAT | _O_EXCL, _S_IREAD | _S_IWRITE);
+}
+#define ftello _ftelli64
+static inline char *ctime_r(const time_t *timep, char *buf) {
+    ctime_s(buf, 26, timep);
+    return buf;
+}
+static inline int unsetenv(const char *name) {
+    return _putenv_s(name, \"\");
+}
+#undef pthread_self
+")
+    
+    # Extract include directories from auto-win-msvc
+    get_target_property(MEGA_INCLUDES auto-win-msvc INTERFACE_INCLUDE_DIRECTORIES)
+    get_filename_component(AUTO_WIN_MSVC_DIR "${CMAKE_CURRENT_SOURCE_DIR}/../auto-win-msvc" ABSOLUTE)
+    file(GLOB POSIX_EXTRA_DIRS "${AUTO_WIN_MSVC_DIR}/posix-*/include" "${AUTO_WIN_MSVC_DIR}/bsd-*/include" "${AUTO_WIN_MSVC_DIR}/linux-*/include")
+    list(FILTER POSIX_EXTRA_DIRS EXCLUDE REGEX "posix-stdatomic")
+    message(WARNING "POSIX_EXTRA_DIRS = ${POSIX_EXTRA_DIRS}")
+    if(MEGA_INCLUDES)
+        include_directories(${MEGA_INCLUDES} ${POSIX_EXTRA_DIRS} ${CMAKE_BINARY_DIR}/include)
+    else()
+        include_directories(${POSIX_EXTRA_DIRS})
+    endif()
+
+    add_compile_options("/FIposix-types.h")
+    add_compile_options("/FI${CMAKE_BINARY_DIR}/msvc_compat.h")
+    add_compile_options("/D__x86_64__" "/DHAVE_PIPE2=1" "/DHAVE_EPOLL=1" "/DHAVE_EPOLL_CREATE=1" "/DHAVE_EPOLL_WAIT=1" "/DHAVE_EPOLL=1" "/DHAVE_EPOLL_CREATE=1" "/DHAVE_EPOLL_WAIT=1" "/wd4273" "/wd4005" "/wd4067" "/D_CRT_SECURE_NO_WARNINGS" "/D_CRT_NONSTDC_NO_WARNINGS" "/D_WINSOCK_DEPRECATED_NO_WARNINGS")
+    set(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} /std:c11 /experimental:c11atomics")
+endif()
 
 set(CMAKE_C_STANDARD 11)
 set(CMAKE_C_STANDARD_REQUIRED ON)
 set(CMAKE_C_EXTENSIONS ON)
 
+file(READ src/server.c CONTENT)
+string(REPLACE "robj objects[argc];" "robj *objects = _alloca(sizeof(robj) * argc);" CONTENT "${CONTENT}")
+string(REPLACE "robj *argv[argc];" "robj **argv = _alloca(sizeof(robj*) * argc);" CONTENT "${CONTENT}")
+string(REPLACE "server.page_size = sysconf(_SC_PAGESIZE);" "server.page_size = 4096;" CONTENT "${CONTENT}")
+file(WRITE "${CMAKE_BINARY_DIR}/patched/src/server.c" "${CONTENT}")
+
+file(READ src/hashtable.c CONTENT)
+string(REPLACE "void *samples[num_samples];" "void **samples = _alloca(sizeof(void*) * num_samples);" CONTENT "${CONTENT}")
+string(REPLACE "bucket *buckets[size];" "bucket **buckets = _alloca(sizeof(bucket*) * size);" CONTENT "${CONTENT}")
+file(WRITE "${CMAKE_BINARY_DIR}/patched/src/hashtable.c" "${CONTENT}")
+file(READ src/valkey-benchmark.c CONTENT)
+string(REPLACE "char data[config.datasize];" "char *data = _alloca(config.datasize);" CONTENT "${CONTENT}")
+file(WRITE "${CMAKE_BINARY_DIR}/patched/src/valkey-benchmark.c" "${CONTENT}")
+file(READ src/networking.c CONTENT)
+string(REPLACE "struct iovec iov_arr[IOV_MAX];" "struct iovec *iov_arr = _alloca(sizeof(struct iovec) * IOV_MAX);" CONTENT "${CONTENT}")
+string(REPLACE "struct iovec iov_arr[iovmax];" "struct iovec *iov_arr = _alloca(sizeof(struct iovec) * iovmax);" CONTENT "${CONTENT}")
+string(REPLACE "char prefixes[iovmax / NUM_OF_IOV_PER_BULK_STR + 1][BULK_STR_LEN_PREFIX_MAX_SIZE];" "char (*prefixes)[BULK_STR_LEN_PREFIX_MAX_SIZE] = _alloca((iovmax / NUM_OF_IOV_PER_BULK_STR + 1) * BULK_STR_LEN_PREFIX_MAX_SIZE);" CONTENT "${CONTENT}")
+string(REPLACE "bufWriteMetadata buf_metadata[replyLen];" "bufWriteMetadata *buf_metadata = _alloca(sizeof(bufWriteMetadata) * replyLen);" CONTENT "${CONTENT}")
+string(REPLACE "hashtableIncrementalFindState key_incr_states[max_keys];" "hashtableIncrementalFindState *key_incr_states = _alloca(sizeof(hashtableIncrementalFindState) * max_keys);" CONTENT "${CONTENT}")
+file(WRITE "${CMAKE_BINARY_DIR}/patched/src/networking.c" "${CONTENT}")
+
+file(READ deps/libvalkey/src/net.c CONTENT)
+string(REPLACE ".connect =" ".posix_connect =" CONTENT "${CONTENT}")
+string(REPLACE ".close =" ".posix_close =" CONTENT "${CONTENT}")
+string(REPLACE ".read =" ".posix_read =" CONTENT "${CONTENT}")
+string(REPLACE ".write =" ".posix_write =" CONTENT "${CONTENT}")
+file(WRITE "${CMAKE_BINARY_DIR}/patched/deps/libvalkey/src/net.c" "${CONTENT}")
+
+file(READ src/sha256.h CONTENT)
+string(REPLACE "WORD" "VALKEY_WORD" CONTENT "${CONTENT}")
+file(WRITE "${CMAKE_BINARY_DIR}/patched/src/sha256.h" "${CONTENT}")
+
+file(READ src/sha256.c CONTENT)
+string(REPLACE "WORD" "VALKEY_WORD" CONTENT "${CONTENT}")
+file(WRITE "${CMAKE_BINARY_DIR}/patched/src/sha256.c" "${CONTENT}")
+
+file(READ src/cluster_migrateslots.c CONTENT)
+string(REPLACE "robj *argv[argc];" "robj **argv = _alloca(sizeof(robj*) * argc);" CONTENT "${CONTENT}")
+file(WRITE "${CMAKE_BINARY_DIR}/patched/src/cluster_migrateslots.c" "${CONTENT}")
+
+file(READ src/cluster_legacy.c CONTENT)
+string(REPLACE "char buf[length + 1];" "char *buf = _alloca(length + 1);" CONTENT "${CONTENT}")
+string(REPLACE "char buf[128];" "char *buf = _alloca(128);" CONTENT "${CONTENT}")
+file(WRITE "${CMAKE_BINARY_DIR}/patched/src/cluster_legacy.c" "${CONTENT}")
+
+file(READ src/valkeymodule.h CONTENT)
+string(REPLACE "(unsigned long)getapifuncptr" "(size_t)getapifuncptr" CONTENT "${CONTENT}")
+file(WRITE "${CMAKE_BINARY_DIR}/patched/src/valkeymodule.h" "${CONTENT}")
+
+file(READ src/module.c CONTENT)
+string(REPLACE "char hyphenfmt[len];" "char *hyphenfmt = _alloca(len);" CONTENT "${CONTENT}")
+string(REPLACE "(unsigned long)VM_" "(size_t)VM_" CONTENT "${CONTENT}")
+string(REPLACE "(unsigned long)&VM_" "(size_t)&VM_" CONTENT "${CONTENT}")
+string(REPLACE "(unsigned long)dlsym" "(size_t)dlsym" CONTENT "${CONTENT}")
+file(WRITE "${CMAKE_BINARY_DIR}/patched/src/module.c" "${CONTENT}")
+
+file(READ src/evict.c CONTENT)
+string(REPLACE "void *samples[server.maxmemory_samples];" "void **samples = _alloca(sizeof(void*) * server.maxmemory_samples);" CONTENT "${CONTENT}")
+file(WRITE "${CMAKE_BINARY_DIR}/patched/src/evict.c" "${CONTENT}")
+
+file(READ src/logreqres.c CONTENT)
+string(REPLACE "\ninline " "\n" CONTENT "${CONTENT}")
+file(WRITE "${CMAKE_BINARY_DIR}/patched/src/logreqres.c" "${CONTENT}")
+
+file(READ src/t_zset.c CONTENT)
+string(REPLACE "_sptr" "_sp" CONTENT "${CONTENT}")
+file(WRITE "${CMAKE_BINARY_DIR}/patched/src/t_zset.c" "${CONTENT}")
+
+file(READ src/util.c CONTENT)
+string(REPLACE "char full_path[PATH_MAX];" "char *full_path = _alloca(4096);" CONTENT "${CONTENT}")
+string(REPLACE "char temp_filename[PATH_MAX];" "char *temp_filename = _alloca(4096);" CONTENT "${CONTENT}")
+file(WRITE "${CMAKE_BINARY_DIR}/patched/src/util.c" "${CONTENT}")
+
+
+include_directories(BEFORE "${CMAKE_BINARY_DIR}/patched/src")
+include_directories(BEFORE "${CMAKE_BINARY_DIR}/patched/deps/libvalkey/src")
+
+
+file(READ deps/linenoise/linenoise.c CONTENT)
+string(REPLACE "const int seqBufferMaxLength = 8;" "#define seqBufferMaxLength 8" CONTENT "${CONTENT}")
+file(WRITE "${CMAKE_BINARY_DIR}/patched/deps/linenoise/linenoise.c" "${CONTENT}")
+
+
+file(COPY src/acl.c DESTINATION "${CMAKE_BINARY_DIR}/patched/src")
+file(COPY src/unit DESTINATION "${CMAKE_BINARY_DIR}/patched/src")
+
+file(READ src/unit/wrappers.h CONTENT)
+string(REPLACE "#define typename _typename" "// removed define typename" CONTENT "${CONTENT}")
+string(REPLACE "#undef typename" "// removed undef typename" CONTENT "${CONTENT}")
+file(WRITE "${CMAKE_BINARY_DIR}/patched/src/unit/wrappers.h" "${CONTENT}")
+
+set(PATCHED_FILES
+    "src/acl.c"
+    "src/unit/main.cpp"
+    "src/unit/test_sha256.cpp"
+    "deps/linenoise/linenoise.c"
+    "src/server.c"
+    "src/hashtable.c"
+    "src/valkey-benchmark.c"
+    "src/networking.c"
+    "src/sha256.h"
+    "src/sha256.c"
+    "src/cluster_migrateslots.c"
+    "src/cluster_legacy.c"
+    "src/valkeymodule.h"
+      "src/module.c"
+    "src/evict.c"
+    "src/logreqres.c"
+    "src/t_zset.c"
+    "src/util.c"
+)
+
 include(ValkeySetup)
+
+foreach(F IN LISTS PATCHED_FILES)
+    if(DEFINED VALKEY_SERVER_SRCS)
+        string(REPLACE "${CMAKE_SOURCE_DIR}/${F}" "${CMAKE_BINARY_DIR}/patched/${F}" VALKEY_SERVER_SRCS "${VALKEY_SERVER_SRCS}")
+    endif()
+    if(DEFINED VALKEY_CLI_SRCS)
+        string(REPLACE "${CMAKE_SOURCE_DIR}/${F}" "${CMAKE_BINARY_DIR}/patched/${F}" VALKEY_CLI_SRCS "${VALKEY_CLI_SRCS}")
+    endif()
+    if(DEFINED VALKEY_BENCHMARK_SRCS)
+        string(REPLACE "${CMAKE_SOURCE_DIR}/${F}" "${CMAKE_BINARY_DIR}/patched/${F}" VALKEY_BENCHMARK_SRCS "${VALKEY_BENCHMARK_SRCS}")
+    endif()
+    if(DEFINED VALKEY_CHECK_RDB_SRCS)
+        string(REPLACE "${CMAKE_SOURCE_DIR}/${F}" "${CMAKE_BINARY_DIR}/patched/${F}" VALKEY_CHECK_RDB_SRCS "${VALKEY_CHECK_RDB_SRCS}")
+    endif()
+    if(DEFINED VALKEY_CHECK_AOF_SRCS)
+        string(REPLACE "${CMAKE_SOURCE_DIR}/${F}" "${CMAKE_BINARY_DIR}/patched/${F}" VALKEY_CHECK_AOF_SRCS "${VALKEY_CHECK_AOF_SRCS}")
+    endif()
+endforeach()
+
+include_directories("${CMAKE_SOURCE_DIR}/src")
 add_subdirectory(src)
 add_subdirectory(tests)
 
@@ -34,17 +418,6 @@ add_subdirectory(tests)
 include(Packaging)
 
 # Clear cached variables from the cache
-unset(BUILD_LUA CACHE)
-unset(BUILD_TESTS CACHE)
-unset(CLANGPP CACHE)
-unset(CLANG CACHE)
-unset(BUILD_RDMA_MODULE CACHE)
-unset(BUILD_TLS_MODULE CACHE)
-unset(BUILD_UNIT_GTESTS CACHE)
-unset(BUILD_TEST_MODULES CACHE)
-unset(BUILD_EXAMPLE_MODULES CACHE)
-unset(USE_TLS CACHE)
-unset(DEBUG_FORCE_DEFRAG CACHE)
 
 # Helper to copy runtest scripts to allow running tests with CMake built binaries
 function(copy_runtest_script script_name)
@@ -75,4 +448,109 @@ copy_runtest_script("runtest")
 copy_runtest_script("runtest-cluster")
 copy_runtest_script("runtest-moduleapi")
 copy_runtest_script("runtest-rdma")
-copy_runtest_script("runtest-sentinel")
\ No newline at end of file
+copy_runtest_script("runtest-sentinel")
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+
+file(APPEND "${CMAKE_BINARY_DIR}/msvc_compat.h" "#define read posix_read
+#define write posix_write
+ssize_t posix_read(int fd, void *buf, size_t count);
+ssize_t posix_write(int fd, const void *buf, size_t count);
+")
+
+file(APPEND "${CMAKE_BINARY_DIR}/msvc_compat.h" "#define _read posix_read
+#define _write posix_write
+ssize_t posix_read(int fd, void *buf, size_t count);
+ssize_t posix_write(int fd, const void *buf, size_t count);
+")
+file(APPEND "${CMAKE_BINARY_DIR}/msvc_compat.h" "\n#define close posix_close\n#define _close posix_close\n#define connect posix_connect\nint posix_close(int fd);\n")
+
+if(WIN32)
+file(WRITE "${CMAKE_BINARY_DIR}/win_kill.c" "
+#include <windows.h>
+#include <stdlib.h>
+#include <string.h>
+
+int main(int argc, char** argv) {
+    if (argc < 2) return 1;
+    
+    int check_only = 0;
+    int pid_idx = 1;
+    
+    if (strcmp(argv[1], \"-0\") == 0) {
+        check_only = 1;
+        pid_idx = 2;
+    } else if (argv[1][0] == '-') {
+        pid_idx = 2;
+    }
+    
+    if (argc <= pid_idx) return 1;
+    int pid = atoi(argv[pid_idx]);
+    
+    if (check_only) {
+        HANDLE h = OpenProcess(SYNCHRONIZE, FALSE, pid);
+        if (!h) return 1;
+        DWORD ret = WaitForSingleObject(h, 0);
+        CloseHandle(h);
+        return ret == WAIT_TIMEOUT ? 0 : 1;
+    } else {
+        HANDLE h = OpenProcess(PROCESS_TERMINATE, FALSE, pid);
+        if (!h) return 1;
+        TerminateProcess(h, 1);
+        CloseHandle(h);
+        return 0;
+    }
+}
+")
+add_executable(win_kill "${CMAKE_BINARY_DIR}/win_kill.c")
+file(WRITE "${CMAKE_BINARY_DIR}/kill.bat" "@echo off\nif exist \"%~dp0bin\\Release\\win_kill.exe\" (\n  \"%~dp0bin\\Release\\win_kill.exe\" %*\n) else if exist \"%~dp0bin\\Debug\\win_kill.exe\" (\n  \"%~dp0bin\\Debug\\win_kill.exe\" %*\n) else (\n  \"%~dp0win_kill.exe\" %*\n)\n")
+endif()
+
+ 
+if(WIN32) 
+    install(FILES valkey.conf DESTINATION bin COMPONENT valkey) 
+    install(PROGRAMS "${CMAKE_CURRENT_SOURCE_DIR}/valkey-service.exe" DESTINATION bin COMPONENT valkey) 
+    install(FILES "${CMAKE_CURRENT_SOURCE_DIR}/valkey-service.xml" DESTINATION bin COMPONENT valkey) 
+endif() 
+file(WRITE "${CMAKE_BINARY_DIR}/posix_stubs/sys/epoll.h" "#include <linux-epoll.h>\n") 
+ 
+if(WIN32) 
+    install(FILES valkey.conf DESTINATION bin COMPONENT valkey) 
+    install(PROGRAMS "${CMAKE_CURRENT_SOURCE_DIR}/valkey-service.exe" DESTINATION bin COMPONENT valkey) 
+    install(FILES "${CMAKE_CURRENT_SOURCE_DIR}/valkey-service.xml" DESTINATION bin COMPONENT valkey) 
+endif() 
+file(WRITE "${CMAKE_BINARY_DIR}/posix_stubs/sys/epoll.h" "#include <linux-epoll.h>\n") 
diff --git a/cmake/Modules/Packaging.cmake b/cmake/Modules/Packaging.cmake
index c7ed5c426..e8ca6b6bf 100644
--- a/cmake/Modules/Packaging.cmake
+++ b/cmake/Modules/Packaging.cmake
@@ -23,7 +23,27 @@ if (DISTRO_NAME MATCHES ubuntu
     set(CPACK_GENERATOR "DEB")
 endif ()
 
-include(CPack)
+if(WIN32) 
+    set(CPACK_GENERATOR "WIX;ZIP;NSIS") 
+    set(CPACK_PACKAGE_NAME "Valkey") 
+    set(CPACK_PACKAGE_VENDOR "Valkey") 
+    set(CPACK_WIX_PATCH_FILE "${CMAKE_CURRENT_SOURCE_DIR}/../packaging/wix_patch.xml") 
+    set(CPACK_WIX_UPGRADE_GUID "68097E99-AC62-42B7-B0E3-02FE50FBB6CB") 
+    set(CPACK_NSIS_EXTRA_INSTALL_COMMANDS "ExecWait '$\\\"$INSTDIR\\\\bin\\\\valkey-service.exe$\\\" install'\nExecWait '$\\\"$INSTDIR\\\\bin\\\\valkey-service.exe$\\\" start'") 
+    set(CPACK_NSIS_EXTRA_UNINSTALL_COMMANDS "ExecWait '$\\\"$INSTDIR\\\\bin\\\\valkey-service.exe$\\\" stop'\nExecWait '$\\\"$INSTDIR\\\\bin\\\\valkey-service.exe$\\\" uninstall'") 
+    set(CPACK_RESOURCE_FILE_LICENSE "${CMAKE_SOURCE_DIR}/COPYING") 
+endif() 
+if(WIN32) 
+    set(CPACK_GENERATOR "WIX;ZIP;NSIS") 
+    set(CPACK_PACKAGE_NAME "Valkey") 
+    set(CPACK_PACKAGE_VENDOR "Valkey") 
+    set(CPACK_WIX_PATCH_FILE "${CMAKE_CURRENT_SOURCE_DIR}/../packaging/wix_patch.xml") 
+    set(CPACK_WIX_UPGRADE_GUID "68097E99-AC62-42B7-B0E3-02FE50FBB6CB") 
+    set(CPACK_NSIS_EXTRA_INSTALL_COMMANDS "ExecWait '$\\\"$INSTDIR\\\\bin\\\\valkey-service.exe$\\\" install'\nExecWait '$\\\"$INSTDIR\\\\bin\\\\valkey-service.exe$\\\" start'") 
+    set(CPACK_NSIS_EXTRA_UNINSTALL_COMMANDS "ExecWait '$\\\"$INSTDIR\\\\bin\\\\valkey-service.exe$\\\" stop'\nExecWait '$\\\"$INSTDIR\\\\bin\\\\valkey-service.exe$\\\" uninstall'") 
+    set(CPACK_RESOURCE_FILE_LICENSE "${CMAKE_SOURCE_DIR}/COPYING.txt") 
+endif() 
+include(CPack)  
 unset(DISTRO_NAME CACHE)
 
 # ---------------------------------------------------
@@ -42,3 +62,5 @@ else                                                        \n\
 fi                                                          \n\
 cd \$PREFIX                                                 \n\
 ln -sf \$1 \$2")
+
+
diff --git a/cmake/Modules/ValkeySetup.cmake b/cmake/Modules/ValkeySetup.cmake
index 4efefefe6..8a0690920 100644
--- a/cmake/Modules/ValkeySetup.cmake
+++ b/cmake/Modules/ValkeySetup.cmake
@@ -50,13 +50,23 @@ endif ()
 
 # Helper function for creating symbolic link so that: link -> source
 macro (valkey_create_symlink source link)
-  add_custom_command(
-    TARGET ${source} POST_BUILD
-    COMMAND ${CMAKE_COMMAND} -E create_symlink
-            "$<TARGET_FILE_NAME:${source}>"
-            "$<TARGET_FILE_DIR:${source}>/${link}"
-    VERBATIM
-  )
+  if(WIN32)
+    add_custom_command(
+      TARGET ${source} POST_BUILD
+      COMMAND ${CMAKE_COMMAND} -E copy
+              "$<TARGET_FILE:${source}>"
+              "$<TARGET_FILE_DIR:${source}>/${link}.exe"
+      VERBATIM
+    )
+  else()
+    add_custom_command(
+      TARGET ${source} POST_BUILD
+      COMMAND ${CMAKE_COMMAND} -E create_symlink
+              "$<TARGET_FILE_NAME:${source}>"
+              "$<TARGET_FILE_DIR:${source}>/${link}"
+      VERBATIM
+    )
+  endif()
 endmacro ()
 
 # Install a binary
@@ -99,7 +109,7 @@ macro (valkey_build_and_install_bin target sources ld_flags libs link_name)
     endif ()
 
     # Enable all warnings + fail on warning
-    target_compile_options(${target} PRIVATE -Werror -Wall)
+    target_compile_options(${target} PRIVATE /W3)
 
     # Install cli tool and create a redis symbolic link
     valkey_install_bin(${target})
@@ -251,11 +261,22 @@ if (VALKEY_DEBUG_BUILD)
 endif ()
 
 # Check for Atomic
+if(MSVC)
+    set(CMAKE_REQUIRED_FLAGS "/std:c11 /experimental:c11atomics")
+endif()
 check_include_files(stdatomic.h HAVE_C11_ATOMIC)
 if (HAVE_C11_ATOMIC)
-    add_valkey_server_compiler_options("-std=gnu11")
+    if(MSVC)
+        add_valkey_server_compiler_options("/std:c11 /experimental:c11atomics")
+    else()
+        add_valkey_server_compiler_options("-std=gnu11")
+    endif()
 else ()
-    add_valkey_server_compiler_options("-std=c99")
+    if(MSVC)
+        add_valkey_server_compiler_options("/std:c11 /experimental:c11atomics")
+    else()
+        add_valkey_server_compiler_options("-std=c99")
+    endif()
 endif ()
 
 # Sanitizer
@@ -291,7 +312,9 @@ if (USE_JEMALLOC)
 endif ()
 
 # Common compiler flags
-add_valkey_server_compiler_options("-pedantic")
+if(NOT MSVC)
+    add_valkey_server_compiler_options("-pedantic")
+endif()
 
 if (NOT BUILD_LUA)
     message(STATUS "Lua scripting engine is disabled")
@@ -315,20 +338,30 @@ if (PYTHON_EXE)
         OUTPUT ${CMAKE_BINARY_DIR}/commands_def_generated
         DEPENDS ${COMMAND_FILES_JSON}
         COMMAND ${PYTHON_EXE} ${CMAKE_SOURCE_DIR}/utils/generate-command-code.py
-        COMMAND touch ${CMAKE_BINARY_DIR}/commands_def_generated
+        COMMAND ${CMAKE_COMMAND} -E touch ${CMAKE_BINARY_DIR}/commands_def_generated
         WORKING_DIRECTORY "${CMAKE_SOURCE_DIR}/src")
     add_custom_target(generate_commands_def DEPENDS ${CMAKE_BINARY_DIR}/commands_def_generated)
 
     # Rule for generating fmtargs.h
     message(STATUS "Adding target generate_fmtargs_h")
-    add_custom_command(
-        OUTPUT ${CMAKE_BINARY_DIR}/fmtargs_generated
-        DEPENDS ${CMAKE_SOURCE_DIR}/utils/generate-fmtargs.py
-        COMMAND sed '/Everything/,$$d' fmtargs.h > fmtargs.h.tmp
-        COMMAND ${PYTHON_EXE} ${CMAKE_SOURCE_DIR}/utils/generate-fmtargs.py >> fmtargs.h.tmp
-        COMMAND mv fmtargs.h.tmp fmtargs.h
-        COMMAND touch ${CMAKE_BINARY_DIR}/fmtargs_generated
-        WORKING_DIRECTORY "${CMAKE_SOURCE_DIR}/src")
+    if (WIN32 AND NOT MSYS AND NOT CYGWIN)
+        add_custom_command(
+            OUTPUT ${CMAKE_BINARY_DIR}/fmtargs_generated
+            DEPENDS ${CMAKE_SOURCE_DIR}/utils/generate-fmtargs.py
+            COMMAND ${PYTHON_EXE} -c "import os; c=open('fmtargs.h', 'r', encoding='utf-8').read(); idx=c.find('Everything'); open('fmtargs.h', 'w', encoding='utf-8').write(c[:idx] if idx != -1 else c)"
+            COMMAND ${PYTHON_EXE} -c "import os, subprocess, sys; out = open('fmtargs.h', 'a', encoding='utf-8', newline='\\n'); subprocess.run([sys.executable, '../utils/generate-fmtargs.py'], stdout=out)"
+            COMMAND ${CMAKE_COMMAND} -E touch ${CMAKE_BINARY_DIR}/fmtargs_generated
+            WORKING_DIRECTORY "${CMAKE_SOURCE_DIR}/src")
+    else ()
+        add_custom_command(
+            OUTPUT ${CMAKE_BINARY_DIR}/fmtargs_generated
+            DEPENDS ${CMAKE_SOURCE_DIR}/utils/generate-fmtargs.py
+            COMMAND sed '/Everything/,$$d' fmtargs.h > fmtargs.h.tmp
+            COMMAND ${PYTHON_EXE} ${CMAKE_SOURCE_DIR}/utils/generate-fmtargs.py >> fmtargs.h.tmp
+            COMMAND mv fmtargs.h.tmp fmtargs.h
+            COMMAND touch ${CMAKE_BINARY_DIR}/fmtargs_generated
+            WORKING_DIRECTORY "${CMAKE_SOURCE_DIR}/src")
+    endif ()
     add_custom_target(generate_fmtargs_h DEPENDS ${CMAKE_BINARY_DIR}/fmtargs_generated)
 else ()
     # Fake targets
@@ -336,11 +369,16 @@ else ()
     add_custom_target(generate_fmtargs_h)
 endif ()
 
-# Generate release.h file (always)
-add_custom_target(
-    release_header
-    COMMAND sh -c '${CMAKE_SOURCE_DIR}/src/mkreleasehdr.sh'
-    WORKING_DIRECTORY "${CMAKE_SOURCE_DIR}/src")
+if (WIN32)
+    file(WRITE ${CMAKE_SOURCE_DIR}/src/release.h "#define REDIS_GIT_SHA1 \"00000000\"\n#define REDIS_GIT_DIRTY \"0\"\n#define REDIS_BUILD_ID \"0\"\n#include \"version.h\"\n#define REDIS_BUILD_ID_RAW SERVER_NAME VALKEY_VERSION REDIS_BUILD_ID REDIS_GIT_DIRTY REDIS_GIT_SHA1\n")
+    add_custom_target(release_header)
+else()
+    # Generate release.h file (always)
+    add_custom_target(
+        release_header
+        COMMAND sh -c '${CMAKE_SOURCE_DIR}/src/mkreleasehdr.sh'
+        WORKING_DIRECTORY "${CMAKE_SOURCE_DIR}/src")
+endif()
 
 # -------------------------------------------------
 # Code Generation section - end
@@ -365,3 +403,5 @@ unset(BUILD_MALLOC CACHE)
 unset(USE_JEMALLOC CACHE)
 unset(BUILD_TLS_MODULE CACHE)
 unset(BUILD_TLS_BUILTIN CACHE)
+
+
diff --git a/deps/libvalkey/CMakeLists.txt b/deps/libvalkey/CMakeLists.txt
index 08c48d63f..ae9620fea 100644
--- a/deps/libvalkey/CMakeLists.txt
+++ b/deps/libvalkey/CMakeLists.txt
@@ -46,7 +46,7 @@ set(valkey_sources
     src/command.c
     src/conn.c
     src/crc16.c
-    src/net.c
+    ${CMAKE_BINARY_DIR}/patched/deps/libvalkey/src/net.c
     src/read.c
     src/sockcompat.c
     src/valkey.c
@@ -100,6 +100,7 @@ if(valkey_compile_definitions)
 endif()
 
 TARGET_INCLUDE_DIRECTORIES(valkey
+    PRIVATE ${CMAKE_CURRENT_SOURCE_DIR}/src
     PUBLIC
         $<INSTALL_INTERFACE:include>
         $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/include/valkey>
@@ -139,7 +140,7 @@ set(CPACK_RPM_PACKAGE_LICENSE "BSD-3-Clause")
 include(CPack)
 
 INSTALL(TARGETS valkey
-    EXPORT valkey-targets
+    
     RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR}
     LIBRARY DESTINATION ${CMAKE_INSTALL_LIBDIR}
     ARCHIVE DESTINATION ${CMAKE_INSTALL_LIBDIR})
@@ -165,9 +166,7 @@ endif()
 INSTALL(FILES ${CMAKE_CURRENT_BINARY_DIR}/valkey.pc
     DESTINATION ${CMAKE_INSTALL_LIBDIR}/pkgconfig)
 
-export(EXPORT valkey-targets
-    FILE "${CMAKE_CURRENT_BINARY_DIR}/valkey-targets.cmake"
-    NAMESPACE valkey::)
+
 
 if(WIN32)
     SET(CMAKE_CONF_INSTALL_DIR share/valkey)
@@ -182,10 +181,7 @@ configure_package_config_file(valkey-config.cmake.in ${CMAKE_CURRENT_BINARY_DIR}
                               INSTALL_DESTINATION ${CMAKE_CONF_INSTALL_DIR}
                               PATH_VARS INCLUDE_INSTALL_DIR)
 
-INSTALL(EXPORT valkey-targets
-        FILE valkey-targets.cmake
-        NAMESPACE valkey::
-        DESTINATION ${CMAKE_CONF_INSTALL_DIR})
+
 
 INSTALL(FILES ${CMAKE_CURRENT_BINARY_DIR}/valkey-config.cmake
               ${CMAKE_CURRENT_BINARY_DIR}/valkey-config-version.cmake
@@ -224,7 +220,7 @@ IF(ENABLE_TLS)
     CONFIGURE_FILE(valkey_tls.pc.in valkey_tls.pc @ONLY)
 
     INSTALL(TARGETS valkey_tls
-        EXPORT valkey_tls-targets
+        
         RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR}
         LIBRARY DESTINATION ${CMAKE_INSTALL_LIBDIR}
         ARCHIVE DESTINATION ${CMAKE_INSTALL_LIBDIR})
@@ -243,9 +239,7 @@ IF(ENABLE_TLS)
     INSTALL(FILES ${CMAKE_CURRENT_BINARY_DIR}/valkey_tls.pc
         DESTINATION ${CMAKE_INSTALL_LIBDIR}/pkgconfig)
 
-    export(EXPORT valkey_tls-targets
-           FILE "${CMAKE_CURRENT_BINARY_DIR}/valkey_tls-targets.cmake"
-           NAMESPACE valkey::)
+    
 
     if(WIN32)
         SET(CMAKE_CONF_INSTALL_DIR share/valkey_tls)
@@ -256,10 +250,7 @@ IF(ENABLE_TLS)
                                   INSTALL_DESTINATION ${CMAKE_CONF_INSTALL_DIR}
                                   PATH_VARS INCLUDE_INSTALL_DIR)
 
-    INSTALL(EXPORT valkey_tls-targets
-        FILE valkey_tls-targets.cmake
-        NAMESPACE valkey::
-        DESTINATION ${CMAKE_CONF_INSTALL_DIR})
+    
 
     INSTALL(FILES ${CMAKE_CURRENT_BINARY_DIR}/valkey_tls-config.cmake
         DESTINATION ${CMAKE_CONF_INSTALL_DIR})
@@ -295,7 +286,7 @@ if(ENABLE_RDMA)
     configure_file(valkey_rdma.pc.in valkey_rdma.pc @ONLY)
 
     install(TARGETS valkey_rdma
-        EXPORT valkey_rdma-targets
+        
         RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR}
         LIBRARY DESTINATION ${CMAKE_INSTALL_LIBDIR}
         ARCHIVE DESTINATION ${CMAKE_INSTALL_LIBDIR})
@@ -307,19 +298,14 @@ if(ENABLE_RDMA)
     install(FILES ${CMAKE_CURRENT_BINARY_DIR}/valkey_rdma.pc
         DESTINATION ${CMAKE_INSTALL_LIBDIR}/pkgconfig)
 
-    export(EXPORT valkey_rdma-targets
-           FILE "${CMAKE_CURRENT_BINARY_DIR}/valkey_rdma-targets.cmake"
-           NAMESPACE valkey::)
+    
 
     set(CMAKE_CONF_INSTALL_DIR ${CMAKE_INSTALL_LIBDIR}/cmake/valkey_rdma)
     configure_package_config_file(valkey_rdma-config.cmake.in ${CMAKE_CURRENT_BINARY_DIR}/valkey_rdma-config.cmake
                                   INSTALL_DESTINATION ${CMAKE_CONF_INSTALL_DIR}
                                   PATH_VARS INCLUDE_INSTALL_DIR)
 
-    install(EXPORT valkey_rdma-targets
-        FILE valkey_rdma-targets.cmake
-        NAMESPACE valkey::
-        DESTINATION ${CMAKE_CONF_INSTALL_DIR})
+    
 
     install(FILES ${CMAKE_CURRENT_BINARY_DIR}/valkey_rdma-config.cmake
         DESTINATION ${CMAKE_CONF_INSTALL_DIR})
diff --git a/deps/linenoise/CMakeLists.txt b/deps/linenoise/CMakeLists.txt
index f801e4abf..6552f7ada 100644
--- a/deps/linenoise/CMakeLists.txt
+++ b/deps/linenoise/CMakeLists.txt
@@ -1,4 +1,5 @@
 project(linenoise)
 
-set(SRCS "${CMAKE_CURRENT_LIST_DIR}/linenoise.c" "${CMAKE_CURRENT_LIST_DIR}/linenoise.h")
+set(SRCS "${CMAKE_BINARY_DIR}/patched/deps/linenoise/linenoise.c" "${CMAKE_CURRENT_LIST_DIR}/linenoise.h")
+include_directories("${CMAKE_CURRENT_LIST_DIR}")
 add_library(linenoise STATIC ${SRCS})
diff --git a/deps/lua/CMakeLists.txt b/deps/lua/CMakeLists.txt
index 6a1396d66..203f27ba3 100644
--- a/deps/lua/CMakeLists.txt
+++ b/deps/lua/CMakeLists.txt
@@ -44,7 +44,9 @@ set(LUA_SRCS
 add_library(lualib STATIC "${LUA_SRCS}")
 target_include_directories(lualib PUBLIC "${LUA_SRC_DIR}")
 target_compile_definitions(lualib PRIVATE ENABLE_CJSON_GLOBAL)
-set(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} -fPIC")
+if(NOT MSVC)
+    set(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} -fPIC")
+endif()
 
 # Use mkstemp if available
 check_function_exists(mkstemp HAVE_MKSTEMP)
diff --git a/src/CMakeLists.txt b/src/CMakeLists.txt
index 9e52edcf5..c15883b7c 100644
--- a/src/CMakeLists.txt
+++ b/src/CMakeLists.txt
@@ -13,6 +13,11 @@ list(APPEND SERVER_LIBS "hdr_histogram")
 list(APPEND SERVER_LIBS "ffc")
 valkey_build_and_install_bin(valkey-server "${VALKEY_SERVER_SRCS}" "${VALKEY_SERVER_LDFLAGS}" "${SERVER_LIBS}"
                              "redis-server")
+if(MSVC)
+    target_link_options(valkey-server PRIVATE "/FORCE:MULTIPLE")
+    target_link_options(valkey-server PRIVATE "/EXPORT:ValkeyModule_OnLoad_lua")
+    target_link_options(valkey-server PRIVATE "/EXPORT:ValkeyModule_OnUnload_lua")
+endif()
 add_dependencies(valkey-server generate_commands_def)
 add_dependencies(valkey-server generate_fmtargs_h)
 add_dependencies(valkey-server release_header)
@@ -75,15 +80,6 @@ valkey_build_and_install_bin(valkey-cli "${VALKEY_CLI_SRCS}" "${VALKEY_SERVER_LD
 add_dependencies(valkey-cli generate_commands_def)
 add_dependencies(valkey-cli generate_fmtargs_h)
 
-# Target: valkey-benchmark
-list(APPEND BENCH_LIBS "fpconv")
-list(APPEND BENCH_LIBS "hdr_histogram")
-list(APPEND BENCH_LIBS "ffc")
-valkey_build_and_install_bin(valkey-benchmark "${VALKEY_BENCHMARK_SRCS}" "${VALKEY_SERVER_LDFLAGS}" "${BENCH_LIBS}"
-                             "redis-benchmark")
-add_dependencies(valkey-benchmark generate_commands_def)
-add_dependencies(valkey-benchmark generate_fmtargs_h)
-
 # Targets: valkey-sentinel, valkey-check-aof and valkey-check-rdb are just symbolic links
 valkey_create_symlink("valkey-server" "valkey-sentinel")
 valkey_create_symlink("valkey-server" "valkey-check-rdb")
@@ -124,15 +119,18 @@ if (BUILD_EXAMPLE_MODULES)
 endif ()
 
 if (BUILD_UNIT_GTESTS)
+    if(NOT MSVC)
     add_subdirectory(unit)
+endif()
 endif ()
 
 # Friendly hint like the Makefile one
 file(RELATIVE_PATH _CMAKE_DIR_RELATIVE_PATH "${CMAKE_SOURCE_DIR}" "${CMAKE_BINARY_DIR}")
-add_custom_target(
-    hint ALL
-    DEPENDS valkey-server valkey-cli valkey-benchmark
+add_custom_target(hint ALL
+    DEPENDS valkey-server valkey-cli 
     COMMAND ${CMAKE_COMMAND} -E echo ""
     COMMAND ${CMAKE_COMMAND} -E echo "Hint: It is a good idea to run tests with your CMake-built binaries \\;\\)"
     COMMAND ${CMAKE_COMMAND} -E echo "      ./${_CMAKE_DIR_RELATIVE_PATH}/runtest"
-    COMMAND ${CMAKE_COMMAND} -E echo "")
+    COMMAND ${CMAKE_COMMAND} -E echo ""
+)
+
diff --git a/src/modules/lua/CMakeLists.txt b/src/modules/lua/CMakeLists.txt
index 6b8d40a39..8c64ce1b8 100644
--- a/src/modules/lua/CMakeLists.txt
+++ b/src/modules/lua/CMakeLists.txt
@@ -1,23 +1,37 @@
 project(valkeylua)
 
 if (VALKEY_DEBUG_BUILD)
-    set(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} -W -Wall -fno-common -g -ggdb -std=c99 -O2 -D_GNU_SOURCE")
+    if (MSVC)
+        set(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} /W3 /Z7 /Od -D_GNU_SOURCE")
+    else()
+        set(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} -W -Wall -fno-common -g -ggdb -std=c99 -O2 -D_GNU_SOURCE")
+    endif()
 else ()
-    set(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} -W -Wall -fno-common -O3 -std=c99  -D_GNU_SOURCE")
-endif ()
+    if (MSVC)
+        set(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} /W3 /O2 -D_GNU_SOURCE")
+    else()
+        set(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} -W -Wall -fno-common -O3 -std=c99  -D_GNU_SOURCE")
+    endif()
+endif()
 
 set(LUA_ENGINE_SRCS
-    engine_lua.c
-    script_lua.c
-    function_lua.c
-    debug_lua.c
-    list.c)
+    ${CMAKE_CURRENT_BINARY_DIR}/patched_engine_lua.c
+    ${CMAKE_CURRENT_BINARY_DIR}/patched_script_lua.c
+    ${CMAKE_CURRENT_BINARY_DIR}/patched_function_lua.c
+    ${CMAKE_CURRENT_BINARY_DIR}/patched_debug_lua.c
+    ${CMAKE_CURRENT_BINARY_DIR}/patched_list.c)
 
 if (BUILD_LUA STREQUAL "module")
     list(APPEND LUA_ENGINE_SRCS ../../sha1.c)
     list(APPEND LUA_ENGINE_SRCS ../../rand.c)
 endif ()
 
+foreach(F engine_lua.c script_lua.c function_lua.c debug_lua.c list.c)
+    file(READ ${F} CONTENT)
+    string(REPLACE "../../valkeymodule.h" "valkeymodule.h" CONTENT "${CONTENT}")
+    file(WRITE "${CMAKE_CURRENT_BINARY_DIR}/patched_${F}" "${CONTENT}")
+endforeach()
+
 if (BUILD_LUA STREQUAL "static")
     message(STATUS "Building STATIC LUA module")
     add_library(valkeylua STATIC "${LUA_ENGINE_SRCS}")
